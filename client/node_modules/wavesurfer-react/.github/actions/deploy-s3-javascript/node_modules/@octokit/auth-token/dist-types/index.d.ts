import type { StrategyInterface, Token, Authentication } from "@octokit/auth-token/dist-types/types";
export type Types = {
    StrategyOptions: Token;
    AuthOptions: never;
    Authentication: Authentication;
};
export declare const createTokenAuth: StrategyInterface;
