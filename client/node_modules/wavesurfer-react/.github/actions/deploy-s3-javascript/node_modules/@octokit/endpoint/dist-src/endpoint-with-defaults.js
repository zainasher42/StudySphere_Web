import { DEFAULTS } from "@octokit/endpoint/dist-src/defaults";
import { merge } from "@octokit/endpoint/dist-src/merge";
import { parse } from "@octokit/endpoint/dist-src/parse";
function endpointWithDefaults(defaults, route, options) {
  return parse(merge(defaults, route, options));
}
export {
  endpointWithDefaults
};
