import { VERSION } from "@octokit/plugin-paginate-rest/dist-src/version.js";
import { paginate } from "@octokit/plugin-paginate-rest/dist-src/paginate.js";
import { iterator } from "@octokit/plugin-paginate-rest/dist-src/iterator.js";
import { composePaginateRest } from "@octokit/plugin-paginate-rest/dist-src/compose-paginate.js";
import {
  isPaginatingEndpoint,
  paginatingEndpoints
} from "@octokit/plugin-paginate-rest/dist-src/paginating-endpoints.js";
function paginateRest(octokit) {
  return {
    paginate: Object.assign(paginate.bind(null, octokit), {
      iterator: iterator.bind(null, octokit)
    })
  };
}
paginateRest.VERSION = VERSION;
export {
  composePaginateRest,
  isPaginatingEndpoint,
  paginateRest,
  paginatingEndpoints
};
