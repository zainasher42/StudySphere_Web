import { withDefaults } from "@octokit/endpoint/dist-src/with-defaults";
import { DEFAULTS } from "@octokit/endpoint/dist-src/defaults";
const endpoint = withDefaults(null, DEFAULTS);
export {
  endpoint
};
