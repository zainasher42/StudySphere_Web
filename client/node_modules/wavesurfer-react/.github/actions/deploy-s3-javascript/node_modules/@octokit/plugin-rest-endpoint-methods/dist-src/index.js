import { VERSION } from "@octokit/plugin-rest-endpoint-methods/dist-src/version.js";
import { endpointsToMethods } from "@octokit/plugin-rest-endpoint-methods/dist-src/endpoints-to-methods.js";
function restEndpointMethods(octokit) {
  const api = endpointsToMethods(octokit);
  return {
    rest: api
  };
}
restEndpointMethods.VERSION = VERSION;
function legacyRestEndpointMethods(octokit) {
  const api = endpointsToMethods(octokit);
  return {
    ...api,
    rest: api
  };
}
legacyRestEndpointMethods.VERSION = VERSION;
export {
  legacyRestEndpointMethods,
  restEndpointMethods
};
